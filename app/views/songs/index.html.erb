<h1>All Songs</h1>
<table>
  <tbody>
    <%@songs.each do |song|%>
    <tr>
      <%= link_to song.title, song_path(song)%></br>
    </tr>
    <%end %>
  </tbody>  
</table>
<%= link_to "Create A New Song", new_song_path%>